# If StenoTray cannot automatically find the log, uncomment the
# next line and customize it to your Plover log path
# session.logFilePath = C:/Users/your_username/AppData/Local/plover/plover/plover.log

# Lesson name
session.lessonName = common_words

# Word dictation. If you enable this, you may also want to disable
# other sounds, by setting "session.isSoundEnabled = false" below,
# though starting from version 0.1.4 sounds don't overlap anymore.
session.isWordDictationEnabled = false

# length of a single session in whole minutes (set to zero to disable timeboxing)
session.timebox = 15

# Base words at the beginning of the lesson (and after resetting it)
session.startBaseWords = 5

# Number of words unlocked at the beginning of the next session
session.unlockedWords = 51

#Starting level at the beginning of the session
session.startLevel = 10

# Increment words at each new level
session.incrementWords = 5

# Required last-x-average WPM for each word for leveling up,
# where x = session.wordAvgSamples
session.minLevelUpWordWpm = 10

# Required average WPM for leveling up
session.minLevelUpTotalWpm = 15

# Samples used for single word wpm average calculation
session.wordAvgSamples = 7

# Word start average WPM
session.wordStartAvgWpm = 9

# whether the target buffer has single or multiple words
session.isSingleWordBuffer = false

# whether sound is enabled or not
session.isSoundEnabled = true

# announce levels
session.isAnnounceLevels = true

# average WPM reporting period in seconds, 0 to disable
session.wpmReportingPeriod = 60

# whether to show on-screen keyboard
session.showKeyboard = true

# whether to show QWERTY keys
session.showKeyboardQwerty = true

# whether to show next chord
session.showKeyboardChord = true
